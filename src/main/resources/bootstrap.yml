spring:
  application:
    name: pocket-operations
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888

base:
  url:
    personalaccount: lb://personal-account
    pocketbook: lb://pocketbook